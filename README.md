I changed the text type to "password" in all fields so that the text would be hidden.
Next step it to make sure that the JavaScript doedsn't run until the HTML page is fully loaded by adding the "DOMContentLoaded" event 
